meta:
  key: Payment_Status
persistence:
  table:
    name: PAYMENT_STATUS
    database: payment
    schema:
      $ref: schema.yaml
    view:
      default:
        $ref: ../../common/default-view.yaml
  topic:
    $ref: ../../common/kafka-topic.yaml
    name: payment.status
pipelines:
  streamliner:
    replicas: 15
name: Payment Status
