meta:
  key: Card_Payment
encryptedPaths:
  - account.number
  - invoice.customerTaxId
  - merchant.taxId
persistence:
  table:
    name: CARD_PAYMENT
    database: payment
    schema:
      $ref: schema.yaml
    view:
      default:
        $ref: view-default.yaml
      settlement:
        $ref: view-settlement.yaml
  topic:
    $ref: ../../common/kafka-topic.yaml
    name: card.payment
pipelines:
  streamliner: [ ]
name: Card Payment
