meta:
  key: Credentials
encryptedPaths:
  - secret
  - password
persistence:
  table:
    name: CREDENTIALS
    database: payment
    schema:
      $ref: schema.yaml
    view:
      default:
        $ref: view-default.yaml
name: Card Payment
