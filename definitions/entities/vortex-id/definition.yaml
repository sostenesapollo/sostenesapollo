meta:
  key: Vortex_ID
persistence:
  table:
    name: VORTEX_ID
    database: payment
    schema:
      $ref: schema.yaml
    view:
      default:
        $ref: view.yaml
  topic:
    $ref: ../../common/kafka-topic.yaml
    name: vortex.id
pipelines:
  streamliner: []
name: Vortex ID
