meta:
  key: Payment_Status_Events
persistence:
  table:
    name: PAYMENT_STATUS_EVENTS
    database: payment
    schema:
      $ref: ../../common/table-schema.yaml
    view:
      default:
        $ref: ../../common/default-view.yaml
  topic:
    $ref: ../../common/kafka-topic.yaml
    name: payment.status.events
pipelines:
  streamliner: []
name: Payment Status Events
